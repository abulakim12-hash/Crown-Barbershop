:root{
  --bg: #0b0c10;
  --panel: #0f1118;
  --panel2: #0c0f16;
  --text: #eef2ff;
  --muted: rgba(238,242,255,.72);
  --muted2: rgba(238,242,255,.56);
  --line: rgba(238,242,255,.10);
  --line2: rgba(238,242,255,.14);
  --accent: #7c5cff;
  --accent2:#23c1ff;
  --shadow: 0 18px 55px rgba(0,0,0,.45);
  --radius: 18px;
  --radius2: 26px;
  --max: 1120px;
}

*{ box-sizing: border-box; }
html{ scroll-behavior:smooth; }
body{
  margin:0;
  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  background: radial-gradient(1200px 800px at 12% 0%, rgba(124,92,255,.18), transparent 60%),
              radial-gradient(900px 700px at 90% 10%, rgba(35,193,255,.14), transparent 55%),
              linear-gradient(180deg, #080910, #070812 55%, #060711);
  color: var(--text);
}

.container{
  width: min(var(--max), calc(100% - 40px));
  margin: 0 auto;
}

a{ color: inherit; text-decoration: none; }
.link{
  color: rgba(238,242,255,.92);
  text-decoration: none;
  border-bottom: 1px solid rgba(238,242,255,.18);
}
.link:hover{ border-bottom-color: rgba(238,242,255,.45); }

.topbar{
  position: sticky;
  top: 0;
  z-index: 50;
  background: rgba(7,8,18,.62);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--line);
}
.topbar__inner{
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding: 10px 0;
  font-size: 13px;
}
.topbar__left{ display:flex; align-items:center; gap:10px; }
.dot{
  width:8px; height:8px; border-radius:50%;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  box-shadow: 0 0 0 4px rgba(124,92,255,.12);
}
.topbar__right{ display:flex; align-items:center; gap:10px; }
.sep{ color: rgba(238,242,255,.35); }

.header{
  position: sticky;
  top: 42px;
  z-index: 40;
  background: rgba(7,8,18,.52);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--line);
}
.header__inner{
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding: 14px 0;
  gap: 14px;
}

.brand{
  display:flex;
  align-items:center;
  gap: 10px;
}
.brand__mark{
  width: 38px; height: 38px;
  display:grid; place-items:center;
  border-radius: 12px;
  background: linear-gradient(135deg, rgba(124,92,255,.95), rgba(35,193,255,.75));
  box-shadow: 0 10px 30px rgba(124,92,255,.18);
  font-weight: 800;
}
.brand__text{ display:flex; flex-direction:column; line-height:1.05; }
.brand__name{ font-weight: 800; letter-spacing: -0.02em; }
.brand__tag{ font-size: 12px; color: var(--muted2); }

.nav{
  display:flex;
  gap: 18px;
  align-items:center;
}
.nav a{
  font-size: 14px;
  color: rgba(238,242,255,.82);
}
.nav a:hover{ color: rgba(238,242,255,.98); }

.btn{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap: 10px;
  padding: 12px 16px;
  border-radius: 14px;
  border: 1px solid rgba(124,92,255,.45);
  background: linear-gradient(135deg, rgba(124,92,255,.95), rgba(35,193,255,.58));
  color: #070812;
  font-weight: 800;
  box-shadow: 0 18px 40px rgba(0,0,0,.38);
}
.btn:hover{ transform: translateY(-1px); }
.btn:active{ transform: translateY(0px); }

.btn--ghost{
  background: rgba(255,255,255,.03);
  border: 1px solid var(--line2);
  color: rgba(238,242,255,.92);
  box-shadow: none;
}
.btn--ghost:hover{
  background: rgba(255,255,255,.06);
}

.header__cta{ display:flex; gap: 10px; align-items:center; }
.hamburger{
  display:none;
  width: 44px;
  height: 44px;
  border-radius: 14px;
  border: 1px solid var(--line2);
  background: rgba(255,255,255,.03);
  color: var(--text);
}
.hamburger span{
  display:block;
  width: 18px;
  height: 2px;
  background: rgba(238,242,255,.85);
  margin: 4px auto;
  border-radius: 99px;
}
.mobile{
  border-top: 1px solid var(--line);
  background: rgba(7,8,18,.72);
  backdrop-filter: blur(10px);
}
.mobile__inner{
  display:flex;
  flex-direction:column;
  gap: 12px;
  padding: 14px 0 18px;
}
.mobile__inner a{
  padding: 10px 12px;
  border-radius: 12px;
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  color: rgba(238,242,255,.90);
}

.hero{ padding: 56px 0 30px; }
.hero__grid{
  display:grid;
  grid-template-columns: 1.05fr .95fr;
  gap: 26px;
  align-items: start;
}
.badge{
  display:inline-flex;
  gap: 10px;
  align-items:center;
  padding: 10px 12px;
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: 999px;
  color: rgba(238,242,255,.88);
  font-size: 13px;
}
.badge__star{
  width: 28px; height: 28px;
  display:grid; place-items:center;
  border-radius: 999px;
  background: rgba(124,92,255,.18);
  border: 1px solid rgba(124,92,255,.30);
  color: rgba(238,242,255,.95);
  font-weight: 900;
}
h1{
  margin: 14px 0 12px;
  font-size: clamp(34px, 4.3vw, 54px);
  line-height: 1.02;
  letter-spacing: -0.04em;
}
.accent{
  background: linear-gradient(135deg, rgba(124,92,255,1), rgba(35,193,255,1));
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}
.lead{
  margin: 0;
  color: var(--muted);
  font-size: 16px;
  line-height: 1.7;
  max-width: 52ch;
}
.hero__actions{ display:flex; gap: 12px; margin-top: 18px; flex-wrap: wrap; }

.stats{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
  margin-top: 22px;
}
.stat{
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: 16px;
  padding: 12px 12px;
}
.stat__num{
  font-weight: 900;
  font-size: 18px;
}
.stat__label{
  font-size: 12px;
  color: var(--muted2);
  margin-top: 2px;
}

.hero__media{ display:flex; flex-direction:column; gap: 12px; }
.mediaCard{
  border-radius: var(--radius2);
  border: 1px solid var(--line);
  background: linear-gradient(180deg, rgba(255,255,255,.05), rgba(255,255,255,.02));
  box-shadow: var(--shadow);
  overflow:hidden;
}
.mediaCard--big{
  padding: 16px;
  min-height: 320px;
  position: relative;
}
.mediaCard__top{ display:flex; justify-content:flex-start; }
.pill{
  display:inline-flex;
  align-items:center;
  padding: 8px 10px;
  border-radius: 999px;
  border: 1px solid rgba(255,255,255,.12);
  background: rgba(0,0,0,.18);
  color: rgba(238,242,255,.86);
  font-size: 12px;
}
.mediaCard__center{
  display:grid;
  place-items:center;
  padding: 34px 0 18px;
}
.mediaCard__logo{
  width: 70px; height: 70px;
  display:grid; place-items:center;
  border-radius: 22px;
  background: linear-gradient(135deg, rgba(124,92,255,.95), rgba(35,193,255,.72));
  font-weight: 900;
  font-size: 28px;
  color: #060711;
  box-shadow: 0 20px 60px rgba(124,92,255,.20);
}
.mediaCard__title{
  margin-top: 12px;
  font-weight: 900;
  letter-spacing: -0.03em;
  font-size: 20px;
}
.mediaCard__sub{
  color: var(--muted2);
  font-size: 13px;
  margin-top: 4px;
}
.mediaCard__bottom{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}
.mini{
  display:flex;
  gap: 10px;
  align-items:center;
  padding: 12px;
  border-radius: 18px;
  border: 1px solid rgba(255,255,255,.10);
  background: rgba(0,0,0,.18);
}
.mini__icon{ font-size: 18px; }
.mini__label{ font-size: 11px; color: var(--muted2); }
.mini__value{ font-size: 13px; font-weight: 700; }

.mediaRow{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}
.mediaCard--small{ padding: 14px; }
.quote__stars{ letter-spacing: 2px; opacity: .95; }
.quote__text{
  margin-top: 10px;
  color: rgba(238,242,255,.88);
  line-height: 1.55;
  font-size: 13px;
}
.quote__name{ margin-top: 10px; color: var(--muted2); font-size: 12px; }

.trust{
  padding: 16px 0 34px;
}
.trust__inner{
  display:grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10px;
}
.trust__item{
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: 18px;
  padding: 14px;
  display:flex;
  gap: 10px;
  align-items:center;
}
.trust__icon{ font-size: 18px; }
.trust__title{ font-weight: 800; font-size: 13px; }
.trust__sub{ color: var(--muted2); font-size: 12px; margin-top: 2px; }

.section{ padding: 54px 0; }
.section--alt{
  background: linear-gradient(180deg, rgba(255,255,255,.02), rgba(255,255,255,.00));
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
}
.section__head{
  display:flex;
  flex-direction:column;
  gap: 8px;
  margin-bottom: 18px;
}
.section__head h2{
  margin:0;
  font-size: 28px;
  letter-spacing: -0.03em;
}
.section__head p{ margin:0; color: var(--muted); max-width: 70ch; line-height:1.65; }

.cards{
  display:grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 12px;
}
.card{
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: 18px;
  padding: 16px;
}
.card__icon{ font-size: 18px; }
.card h3{
  margin: 10px 0 6px;
  letter-spacing: -0.02em;
}
.card p{ margin:0; color: var(--muted); line-height:1.6; font-size: 14px; }

.reviewHeader{
  display:flex;
  gap: 16px;
  align-items:flex-start;
  justify-content:space-between;
  flex-wrap: wrap;
  margin-bottom: 14px;
}
.reviewScore{
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: 18px;
  padding: 14px 16px;
  min-width: 260px;
}
.reviewScore__big{ font-size: 34px; font-weight: 900; letter-spacing: -0.03em; }
.reviewScore__stars{ letter-spacing: 3px; margin-top: 2px; opacity: .95; }
.reviewScore__meta{ color: var(--muted2); margin-top: 6px; font-size: 13px; }

.reviewTags{ display:flex; gap: 8px; flex-wrap: wrap; justify-content:flex-end; }
.tag{
  padding: 8px 10px;
  border: 1px solid rgba(255,255,255,.12);
  background: rgba(0,0,0,.18);
  border-radius: 999px;
  color: rgba(238,242,255,.86);
  font-size: 12px;
}

.slider{
  display:grid;
  grid-template-columns: 44px 1fr 44px;
  gap: 10px;
  align-items:center;
}
.slider__btn{
  width: 44px; height: 44px;
  border-radius: 14px;
  border: 1px solid var(--line2);
  background: rgba(255,255,255,.03);
  color: rgba(238,242,255,.92);
  font-size: 22px;
  cursor:pointer;
}
.slider__btn:hover{ background: rgba(255,255,255,.06); }

.slider__track{
  display:flex;
  gap: 12px;
  overflow:hidden;
  scroll-behavior:smooth;
}
.review{
  min-width: min(520px, 100%);
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: 18px;
  padding: 16px;
}
.review__top{
  display:flex;
  flex-direction:column;
  gap: 6px;
}
.review__stars{ letter-spacing: 2px; opacity: .95; }
.review__meta{ color: var(--muted2); font-size: 12px; }
.review__text{ margin: 10px 0 0; color: rgba(238,242,255,.90); line-height: 1.65; }
.review__sub{ margin: 10px 0 0; color: var(--muted2); font-size: 12px; }

.sliderDots{
  display:flex;
  gap: 8px;
  justify-content:center;
  margin-top: 14px;
}
.dotBtn{
  width: 9px; height: 9px;
  border-radius: 999px;
  border: 1px solid rgba(255,255,255,.20);
  background: rgba(255,255,255,.08);
  cursor:pointer;
}
.dotBtn[aria-current="true"]{
  background: linear-gradient(135deg, rgba(124,92,255,1), rgba(35,193,255,1));
  border-color: transparent;
}

.pricing{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
}
.priceCard{
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: 18px;
  padding: 18px;
  position: relative;
}
.priceCard--featured{
  border-color: rgba(124,92,255,.40);
  background: linear-gradient(180deg, rgba(124,92,255,.12), rgba(255,255,255,.03));
}
.priceCard__badge{
  position:absolute;
  top: 14px; right: 14px;
  padding: 8px 10px;
  border-radius: 999px;
  border: 1px solid rgba(124,92,255,.35);
  background: rgba(124,92,255,.14);
  font-size: 12px;
  color: rgba(238,242,255,.92);
}
.priceCard__title{ font-weight: 800; letter-spacing: -0.02em; }
.priceCard__price{
  font-size: 34px;
  font-weight: 900;
  letter-spacing: -0.03em;
  margin-top: 8px;
}
.priceCard__list{ margin: 12px 0 0; padding-left: 18px; color: var(--muted); line-height:1.8; }

.book{
  display:grid;
  grid-template-columns: 1.1fr .9fr;
  gap: 14px;
  align-items:start;
}
.note{
  margin-top: 14px;
  padding: 12px 14px;
  border-radius: 16px;
  border: 1px solid rgba(255,255,255,.12);
  background: rgba(0,0,0,.18);
  color: rgba(238,242,255,.86);
  line-height:1.55;
}
.hours{
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: 18px;
  padding: 16px;
}
.hours__head{ display:flex; gap: 10px; align-items:center; }
.hours__icon{ font-size: 18px; }
.hours__title{ font-weight: 900; letter-spacing:-0.02em; }
.hours__sub{ color: var(--muted2); font-size: 12px; margin-top: 2px; }
.hours__grid{ margin-top: 12px; display:flex; flex-direction:column; gap: 8px; }
.hours__row{
  display:flex;
  justify-content:space-between;
  gap: 10px;
  padding: 10px 10px;
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,.08);
  background: rgba(0,0,0,.15);
  color: rgba(238,242,255,.88);
  font-size: 13px;
}
.hours__row span:first-child{ color: var(--muted2); }

.location{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
  align-items: stretch;
}
.location__card{
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  border-radius: 18px;
  padding: 18px;
}
.muted{ color: var(--muted); line-height:1.65; }
.info{ margin-top: 14px; display:flex; flex-direction:column; gap: 10px; }
.info__row{
  display:flex;
  justify-content:space-between;
  gap: 10px;
  padding: 10px 10px;
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,.08);
  background: rgba(0,0,0,.15);
}
.info__label{ color: var(--muted2); font-size: 13px; }
.info__value{ color: rgba(238,242,255,.90); font-size: 13px; }
.location__actions{ margin-top: 14px; display:flex; gap: 10px; flex-wrap: wrap; }

.mapWrap{
  border-radius: 18px;
  overflow:hidden;
  border: 1px solid var(--line);
  background: rgba(255,255,255,.03);
  min-height: 360px;
}
.mapWrap iframe{
  width: 100%;
  height: 100%;
  border: 0;
}

.footer{
  border-top: 1px solid var(--line);
  padding: 22px 0;
  background: rgba(0,0,0,.18);
}
.footer__inner{
  display:flex;
  gap: 14px;
  align-items:center;
  justify-content:space-between;
  flex-wrap: wrap;
}
.footer__brand{
  display:flex;
  gap: 10px;
  align-items:center;
}
.footer__name{ font-weight: 900; letter-spacing:-0.02em; }
.footer__sub{ color: var(--muted2); font-size: 12px; margin-top: 2px; }
.footer__links{ display:flex; gap: 12px; flex-wrap: wrap; }
.footer__links a{ color: rgba(238,242,255,.72); font-size: 13px; }
.footer__links a:hover{ color: rgba(238,242,255,.95); }
.footer__copy{ color: rgba(238,242,255,.55); font-size: 12px; }

/* Responsive */
@media (max-width: 980px){
  .hero__grid{ grid-template-columns: 1fr; }
  .trust__inner{ grid-template-columns: repeat(2, 1fr); }
  .cards{ grid-template-columns: repeat(2, 1fr); }
  .pricing{ grid-template-columns: 1fr; }
  .book{ grid-template-columns: 1fr; }
  .location{ grid-template-columns: 1fr; }
  .nav, .header__cta{ display:none; }
  .hamburger{ display:inline-block; }
}
@media (max-width: 520px){
  .stats{ grid-template-columns: 1fr; }
  .mediaRow{ grid-template-columns: 1fr; }
  .slider{ grid-template-columns: 40px 1fr 40px; }
  .review{ min-width: 100%; }
}
